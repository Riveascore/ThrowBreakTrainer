<%= form_for(@video_info) do |f| %>
  <% if @video_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_info.errors.count, "error") %> prohibited this video_info from being saved:</h2>

      <ul>
      <% @video_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :video_name %><br>
    <%= f.text_field :video_name %>
  </div>
  <div class="field">
    <%= f.label :number_of_milliseconds %><br>
    <%= f.number_field :number_of_milliseconds %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
